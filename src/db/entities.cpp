#include "entities.h"

namespace DB
{
	const Entity g_Auditoriums("Auditoriums", true);
	const Entity g_Days("Days", false);
	const Entity g_Hours("Hours", false);
	const Entity g_Groups("Groups", true);
	const Entity g_Teachers("Teachers", true);
	const Entity g_Lessons("Lessons", true);
}

